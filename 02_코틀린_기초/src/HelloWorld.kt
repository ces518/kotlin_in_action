/**
 * 1. 함수 선언시 fun 키워드를 사용한다.
 * 2. 파라미터 명 뒤에 해당 파라미터의 타입을 쓴다
 * ex) args: Array<String>
 * 3. 함수를 최상위 수준에 정의할 수 있다.
 * 4. 배열도 일반클래스와 동일하게 취급한다. 자바와 달리 배열처리 문법이 따로 존재하지 않는다.
 * 5. System.out.println 대신 println 을 사용한다.
 * 코틀린 표준 라이브러리는 자바 표준 라이브러리를 간결하게 사용가능하게 감싼 wrapper 를 제공한다.
 * 6. 문장 마지막에 세미콜론을 붙이지 않아도 된다.
 */
fun main (args: Array<String>) {
    println("Hello World")
}